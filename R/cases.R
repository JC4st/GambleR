# Educational cases are kept separate from the physiological engine.

educational_cases <- list(
  reference = list(
    id = "reference",
    values = list(
      sodium = 140,
      potassium = 4,
      chloride = 104,
      bicarbonate = 24,
      albumin = 4,
      lactate = 1,
      ionized_calcium = 1.2
    )
  )
)
